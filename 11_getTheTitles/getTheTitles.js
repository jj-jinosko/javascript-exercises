const getTheTitles = function(arr) {
    return arr.map((obj)=>obj.title);
};

// Do not edit below this line
module.exports = getTheTitles;

// array of objects
// filter =array from t/f or map=new array

// could flatten and just get book titles? no bc keys would be duplicated...?

// look at each element
// select book.title property?